source "https://rubygems.org"
gemspec

gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]

# bundle update
# bundle exec jekyll serve
# bundle exec jekyll serve --livereload

# js uglify
# npm install -g uglify-js (only once)
# npm run uglify